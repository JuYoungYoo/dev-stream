/*
package com.devinfo.web.controller;

import com.devinfo.sns.domain.Shops;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.boot.test.autoconfigure.web.client.RestClientTest;
import org.springframework.boot.test.context.SpringBootTest;
import org.springframework.boot.test.web.client.TestRestTemplate;
import org.springframework.http.HttpStatus;
import org.springframework.http.ResponseEntity;
import org.springframework.test.context.junit4.SpringRunner;
import org.springframework.test.web.client.MockRestServiceServer;
import org.springframework.web.client.RestTemplate;

import java.util.Collection;
import java.util.List;

import static org.hamcrest.Matchers.is;
import static org.junit.Assert.*;
import static org.springframework.boot.test.context.SpringBootTest.WebEnvironment.RANDOM_PORT;

@RunWith(SpringRunner.class)
@SpringBootTest(webEnvironment = RANDOM_PORT)
public class MainControllerTest {


    private String snsApiUrl = "http://localhost:8081/sns";

    @Autowired
    private TestRestTemplate restTemplate;

    @Test
    public void 메인페이지_로딩() {
        // when
        //when
        String body = this.restTemplate.getForObject("/", String.class);

*/
/*

        ResponseEntity<List> responseEntity = restTemplate.getForEntity(snsApiUrl + "/sns", List.class);
        List<Shops> body = responseEntity.getBody();

        // given
        // then
        assertThat(responseEntity.getStatusCode(), is(HttpStatus.OK));
        assertThat(body.get(0).getName(), is("jojoldu"));
*//*


   }

}*/
